# Sandbox

The backend uses `python:3.11-slim` directly for the MVP test sandbox.

For production/hackathon infrastructure, replace this with a prebuilt image containing
the repository's dependency installation and enforce stronger isolation at the worker/VM level.
